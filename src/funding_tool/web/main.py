"""funding-tool Web UI FastAPI application — placeholder.

The full FastAPI app is assembled in Task 6 (config + lifespan + middleware
装配 + master key 加载). This stub keeps the `funding-tool-web` console script
entry resolvable so `pip install`/`uv sync` resolves cleanly. Invoking the
script before Task 6 lands will raise NotImplementedError.
"""
from __future__ import annotations


def run() -> None:
    raise NotImplementedError(
        "funding-tool-web is not yet implemented; see Task 6 of the web UI plan"
    )
